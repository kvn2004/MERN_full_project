import { Request, Response } from "express";

export const addPartner = async (req: Request, res: Response) => {
  res.json({ message: "Add partner - TODO" });
};

export const getAllPartners = async (req: Request, res: Response) => {
  res.json({ message: "Get all partners - TODO" });
};

export const getPartner = async (req: Request, res: Response) => {
  res.json({ message: "Get partner - TODO" });
};

export const updatePartner = async (req: Request, res: Response) => {
  res.json({ message: "Update partner - TODO" });
};

export const deletePartner = async (req: Request, res: Response) => {
  res.json({ message: "Delete partner - TODO" });
};

export const addPartnerCycle = async (req: Request, res: Response) => {
  res.json({ message: "Add partner cycle - TODO" });
};

export const getPartnerCycles = async (req: Request, res: Response) => {
  res.json({ message: "Get partner cycles - TODO" });
};

export const predictPartnerCycle = async (req: Request, res: Response) => {
  res.json({ message: "Predict partner cycle - TODO" });
};
